{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "typecheck": "pnpm run typecheck:libs && pnpm -r --filter \"./artifacts/portal\" --if-present run typecheck",
    "build": "pnpm -C artifacts/portal run build",
    "deploy": "gh-pages -d artifacts/portal/dist"
  },
  "private": true,
  "devDependencies": {
    "gh-pages": "^6.3.0",
    "prettier": "^3.8.3",
    "typescript": "~5.9.3"
  }
}
